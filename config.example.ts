// IMPORTANT: Create a copy of this file named `config.ts` and paste your actual API key.
// The `config.ts` file is already in .gitignore, so it will not be uploaded to GitHub.
export const GEMINI_API_KEY = "PASTE_YOUR_GEMINI_API_KEY_HERE";
